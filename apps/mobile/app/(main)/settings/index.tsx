import { View, Text } from 'react-native';

// TODO: Settings Screen
// - Preferences (gender, age range, distance, height)
// - Link to privacy settings, blocked users
// - Logout / delete account
export default function SettingsScreen() {
  return (
    <View>
      <Text>Settings</Text>
    </View>
  );
}

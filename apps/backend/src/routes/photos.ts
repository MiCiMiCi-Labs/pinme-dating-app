import { Router } from 'express';

// GET    /api/v1/photos
// POST   /api/v1/photos         (upload)
// DELETE /api/v1/photos/:id
// PATCH  /api/v1/photos/:id/order
// PATCH  /api/v1/photos/:id/primary

const router = Router();

export default router;
